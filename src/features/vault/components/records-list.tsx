import { PublicRecordType } from '@/lib/schema'

// MUST USE ./record-row.tsx for each record row

export function RecordsList({ records }: { records: PublicRecordType[] }) {
  return <div>vault-records-table</div>
}
